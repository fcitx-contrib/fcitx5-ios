# What does full access do for a keyboard?

With full access, a keyboard can:
* write to App Group (read is always allowed, as long as not side load);
* access network, including localhost (that's why we need it for side load to sync config from main app) and internet (cloud pinyin);
* access system clipboard on real device.
